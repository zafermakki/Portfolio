import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Portfolio - Professional Web Developer",
  description: "Professional portfolio of a web developer specializing in Next.js, React, TypeScript and Tailwind CSS",
  keywords: "portfolio, web developer, nextjs, react, typescript, tailwind",
  authors: [{ name: "Your Name" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" dir="ltr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        style={{ direction: 'ltr', textAlign: 'left' }}
      >
        {children}
      </body>
    </html>
  );
}
